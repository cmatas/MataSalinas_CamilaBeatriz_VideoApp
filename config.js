var config = {
  user : "root",
  password : "", //leave blank for windows
  // port: 8889,
  host: "localhost",
  database: "db_videoapp", // database

  kidsmode : false
}

module.exports = config;
